// Toggle this during development
window.CA_CONFIG = {
  SIMULATE: true,                // true = no backend needed; local demo works
  API_BASE: "http://localhost:8080", // when backend ready, set SIMULATE:false
};
